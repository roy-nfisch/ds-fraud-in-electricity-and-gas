# Local notebook configuration
# Copy this file to .env, then change values there. Never commit .env.

# Save the selected features and model train/test CSVs after running 01_eda.ipynb.
SAVE_CSV_FILE=true
